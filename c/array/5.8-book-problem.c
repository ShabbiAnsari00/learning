#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main (void)
{
    char a[15];
    int i, j, k, len;

    printf("Enter a binary number: ");
    scanf("%[^\n]", a);
    len = strlen(a);

    for(k=0; k<)

}